<div class="container">
    <h1>みんなの診断結果</h1>

    <% @results.each_with_index do |result, index| %>
        <div class="mb-3">
        <% if result.user.release_option == 0 %>
            <span class="badge bg-primary"><%= index + 1 %></span>
            <%= link_to "#{result.user.username}: #{result.user.created_at.strftime('%Y-%m-%d')}", user_path(result.user), class: "user_show_path" %>
        <%end%>
        </div>
    <% end %>
</div>
